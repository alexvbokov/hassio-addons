{
  "name": "unix server state",
  "version": "0.20",
  "slug": "unix_server_state",
  "hassio_role": "default",
  "init": "false",
  "homeassistant_api": "true",
  "hassio_api": "true",
  "hassio_role": "default",
  "description": "xNIX server state by ssh (с) Alex Bokov",
  "arch": ["armhf", "armv7", "aarch64", "amd64", "i386"],
  "startup": "application",
  "boot": "auto",
  "options": { 
	"servers": "
	{
		\"192.168.0.106\": {
			\"login\": \"root\",
			\"password\": \"root\",
			\"sensors\": {
				\"microserver_cpu0_temp\": \"sysctl -a | grep temperature | grep cpu.0 | awk '{print substr($2,1,length($2)-3)}'\",
				\"microserver_cpu1_temp\": \"sysctl -a | grep temperature | grep cpu.1 | awk '{print substr($2,1,length($2)-3)}'\"
			}
		},
		\"192.168.0.108\": {
			\"login\": \"root\",
			\"password\": \"root\",
			\"sensors\": {
				\"istorage_cpu0_temp\": \"sysctl -a | grep temperature | grep cpu.0 | awk '{print substr($2,1,length($2)-3)}'\",
				\"istorage_cpu1_temp\": \"sysctl -a | grep temperature | grep cpu.1 | awk '{print substr($2,1,length($2)-3)}'\"
			}
		}
	}"	},
  "schema": { 
	"servers": "str"
}